apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "quree-scan-miniapp.fullname" . }}-test-connection"
  labels:
    {{- include "quree-scan-miniapp.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "quree-scan-miniapp.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
