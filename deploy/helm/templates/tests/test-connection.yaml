apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "quree.fullname" . }}-test-connection"
  labels:
    {{- include "quree.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "quree.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
