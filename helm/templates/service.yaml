apiVersion: v1
kind: Service
metadata:
  name: {{ include "quree-scan-miniapp.fullname" . }}
  labels:
    {{- include "quree-scan-miniapp.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "quree-scan-miniapp.selectorLabels" . | nindent 4 }}
